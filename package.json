{
  "name": "mr-utc",
  "version": "1.0.0",
  "description": "Railway-ready WhatsApp bot for Mr. UTC using whatsapp-web.js and persistent local storage",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "start:fresh": "node scripts/start-fresh.js",
    "session:export": "node scripts/export-session.js",
    "dev": "node bot.js",
    "test": "node --check bot.js && node --check scripts/start-fresh.js && node --check scripts/export-session.js && node --check src/app.js && node --check src/config.js && node --check src/store.js && node --check src/copy.js && node --check src/session-bundle.js"
  },
  "keywords": [],
  "author": "Mr. UTC",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.6"
  }
}
